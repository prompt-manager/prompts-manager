import styled from "styled-components";

export const S_NotFound = styled.div`
    margin-top: var(--spacing-09);
    text-align: center;
    font-size: 5.6rem;
    color: var(--text-error);
`
S_NotFound.displayName = 'S_NotFound'
