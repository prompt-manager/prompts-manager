import styled from 'styled-components'
import { Divider, DividerProps } from 'antd'

export const S_Divider = styled(Divider)<DividerProps>`
    background: var(--border-primary);
`
S_Divider.displayName = 'S_Divider'
